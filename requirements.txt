pymupdf>=1.24
python-docx>=1.1
docx2pdf>=0.1.8        # 선택: docx→pdf 자동 변환. Word가 있으면 이 방식으로 동작.
                       # LibreOffice(soffice)가 있으면 그쪽을 먼저 시도하므로 없어도 됩니다.
# 아래 둘 중 가진 키에 맞는 것만 있어도 됩니다. (둘 다 깔아두면 둘 다 선택 가능)
anthropic>=0.40        # Anthropic(Claude) API 사용 시
openai>=1.40           # OpenAI(GPT) API 사용 시
